let ctx:AudioContext|null=null,master:GainNode|null=null,timer:ReturnType<typeof setInterval>|undefined;let active=false;
export function setSound(on:boolean){active=on;if(!on){master?.gain.setTargetAtTime(0,ctx!.currentTime,.2);clearInterval(timer);return;}ctx??=new AudioContext();void ctx.resume();if(!master){master=ctx.createGain();master.connect(ctx.destination);const buffer=ctx.createBuffer(1,ctx.sampleRate*3,ctx.sampleRate);const data=buffer.getChannelData(0);let last=0;for(let i=0;i<data.length;i++){last=(last+(Math.random()*2-1)*.025)/1.025;data[i]=last*2;}const noise=ctx.createBufferSource();noise.buffer=buffer;noise.loop=true;const filter=ctx.createBiquadFilter();filter.type='lowpass';filter.frequency.value=700;noise.connect(filter).connect(master);noise.start();}master.gain.setTargetAtTime(.12,ctx.currentTime,.2);clearInterval(timer);timer=setInterval(()=>{if(document.hidden)return;const c=ctx!,o=c.createOscillator(),g=c.createGain(),t=c.currentTime;o.type='sine';o.frequency.setValueAtTime(1600+Math.random()*900,t);o.frequency.exponentialRampToValueAtTime(2800,t+.13);o.frequency.exponentialRampToValueAtTime(1700,t+.3);g.gain.setValueAtTime(0,t);g.gain.linearRampToValueAtTime(.08,t+.04);g.gain.exponentialRampToValueAtTime(.001,t+.35);o.connect(g).connect(master!);o.start(t);o.stop(t+.4);},3700);}
export function chime(){if(!active||!ctx||!master)return;for(let i=0;i<2;i++){const o=ctx.createOscillator(),g=ctx.createGain(),t=ctx.currentTime+i*.1;o.frequency.value=i?880:659;g.gain.setValueAtTime(.18,t);g.gain.exponentialRampToValueAtTime(.001,t+.22);o.connect(g).connect(master);o.start(t);o.stop(t+.25);}}
